import 'package:fludget/catalog/widget_def.dart';
import 'package:fludget/catalog/widgets/layout/sized_box.dart';

/// Every built-in widget in the Layout category. Adding a layout widget means
/// writing its def file and appending it here — the only edit outside the def.
final List<WidgetDef> layoutDefs = [
  sizedBoxDef,
];
